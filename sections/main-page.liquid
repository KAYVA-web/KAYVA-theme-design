<section class="section">
  <div class="page-width--narrow">
    <h1 class="section-heading" style="margin-bottom:var(--spacing-lg);">{{ page.title }}</h1>
    <div style="font-size:15px; line-height:1.8; color:var(--color-ink);">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page Content", "settings": [] }
{% endschema %}
