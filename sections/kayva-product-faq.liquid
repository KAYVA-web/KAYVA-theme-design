{{ 'kayva.css' | asset_url | stylesheet_tag }}
<style>
.kv-faq { padding: 64px 0; background: {{ section.settings.background_color }}; }
.kv-faq__heading {
  font-family: var(--kayva-font);
  font-size: 22px;
  font-weight: 600;
  color: var(--kayva-dark-brown);
  margin-bottom: 32px;
}
.kv-faq__list { border-top: 1px solid var(--kayva-line); }
.kv-faq__item {
  border-bottom: 1px solid var(--kayva-line);
}
.kv-faq__item summary {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px 0;
  font-family: var(--kayva-font);
  font-size: 15px;
  font-weight: 500;
  color: var(--kayva-dark-brown);
  cursor: pointer;
  list-style: none;
  user-select: none;
}
.kv-faq__item summary::-webkit-details-marker { display: none; }
.kv-faq__item summary svg {
  flex-shrink: 0;
  transition: transform 0.25s;
  color: var(--kayva-warm-brown);
}
.kv-faq__item[open] summary svg { transform: rotate(180deg); }
.kv-faq__answer {
  font-family: var(--kayva-font);
  font-size: 14px;
  color: var(--kayva-warm-brown);
  line-height: 1.7;
  padding-bottom: 18px;
}
.kv-faq__answer a { color: var(--kayva-amber); text-decoration: underline; }
</style>

<section class="kv-faq" id="kv-faq-{{ section.id }}">
  <div class="kayva-page-width">
    {%- if section.settings.heading != blank -%}
      <h2 class="kv-faq__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <div class="kv-faq__list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'qa' -%}
            <details class="kv-faq__item" {{ block.shopify_attributes }}>
              <summary>
                {{ block.settings.question }}
                <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                  <path d="M4.5 6.75L9 11.25L13.5 6.75" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </summary>
              <div class="kv-faq__answer">{{ block.settings.answer }}</div>
            </details>
          {%- endif -%}
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "KAYVA Product FAQ",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Frequently asked questions"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background colour",
      "default": "#FFFFFF"
    }
  ],
  "blocks": [
    {
      "type": "qa",
      "name": "Q&A Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "Enter your question here"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Enter your answer here.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KAYVA Product FAQ",
      "blocks": [
        { "type": "qa", "settings": { "question": "What is included in the box?", "answer": "<p>Enter your answer here.</p>" } },
        { "type": "qa", "settings": { "question": "Is this product dimmable?", "answer": "<p>Enter your answer here.</p>" } }
      ]
    }
  ]
}
{% endschema %}
